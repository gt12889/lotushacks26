# Ralph Progress - 21st.dev + ReactBits Component Migration
# Started: 2026-03-21
# Branch: ralph/21st-reactbits-components

## Codebase Patterns
- Frontend uses Tailwind v4 with `@tailwindcss/postcss` — no tailwind.config file
- Custom theme colors defined via `@theme inline` block in globals.css (not tailwind.config)
- Path alias `@/*` maps to `./src/*` in tsconfig.json
- shadcn v4.1.0 uses `base-nova` style (not `new-york`/`default` — those are removed in v4+)
- shadcn init with `-d -y` flags for non-interactive defaults
- App is always dark-themed — no .dark class toggle needed, :root vars set to abyss theme directly
- Components in src/components/ use mixed import styles: relative (`./Foo`) for siblings, alias (`@/components/Foo`) from app pages — replace both when swapping components
- shadcn Badge v4 uses `useRender` from `@base-ui/react` — pass `className` overrides to customize colors
- shadcn Collapsible v4 uses `defaultOpen` prop and manages state internally — no useState needed for expand/collapse
- 21st.dev registry may return 500 errors — implement ReactBits components manually when needed
- Custom Counter component at ui/counter.tsx provides animated number transitions (easeOutExpo, requestAnimationFrame)

## 2026-03-21 - US-001
- Initialized shadcn/ui via `npx shadcn@latest init -d -y`
- Customized shadcn CSS variables in :root to match abyss dark theme colors
- Preserved all custom animations and theme variables in globals.css
- Files changed: globals.css, components.json, src/lib/utils.ts, src/components/ui/button.tsx, package.json, package-lock.json
- **Learnings for future iterations:**
  - shadcn v4+ no longer supports `--style` flag — uses `--preset` instead; `-d` gives `base-nova` preset
  - shadcn init merges with existing globals.css rather than overwriting — appends :root/.dark vars at the bottom
  - The app body colors are set via plain CSS (`body { background-color: #0D1C32; color: #D6E3FF; }`) not @apply
  - clsx and tailwind-merge are auto-installed as dependencies by shadcn init
---

## 2026-03-21 - US-002
- Installed shadcn Badge component via `npx shadcn@latest add badge -y`
- Created MegalodonBadge wrapper in src/components/ui/megalodon-badge.tsx mapping all 7 status types to Badge with outline variant and custom theme colors
- Replaced all StatusPill imports/usages in PharmacyCards.tsx, PriceGrid.tsx, DashboardHome.tsx, alerts/page.tsx
- Deleted StatusPill.tsx
- Files changed: ui/badge.tsx (new), ui/megalodon-badge.tsx (new), PharmacyCards.tsx, PriceGrid.tsx, DashboardHome.tsx, alerts/page.tsx, StatusPill.tsx (deleted)
- **Learnings for future iterations:**
  - shadcn v4 Badge uses `useRender` from `@base-ui/react` — different API than older shadcn versions
  - StatusPill was used in 4 files with 8 total usages — always grep broadly when replacing components
  - Some files import components with relative paths (`./StatusPill`), others use alias (`@/components/StatusPill`) — replace both patterns
  - MegalodonBadge preserves the same prop interface (status, label) for drop-in replacement
---

## 2026-03-21 - US-003
- Refactored SponsorBadge.tsx to use shadcn Badge with outline variant and custom sponsor colors via inline style
- Refactored ReferencePriceBadge.tsx to wrap WHO price display in shadcn Badge with outline variant
- Added Supermemory (#8B5CF6) to SPONSOR_COLORS map (8th sponsor per PRD)
- All USD→VND conversion logic and ratio indicator preserved
- Files changed: SponsorBadge.tsx, ReferencePriceBadge.tsx
- **Learnings for future iterations:**
  - Badge `h-auto` override is needed when Badge content has custom padding — default h-5 constrains content
  - SponsorBadge is imported in 5 files (PriceGrid, PharmacyCards, ModelRouterPanel, CounterfeitRiskPanel, ReferencePriceBadge) — refactoring in-place avoids touching all consumers
  - ReferencePriceBadge internally uses SponsorBadge for the Exa attribution — nesting Badge inside Badge works fine
---

## 2026-03-21 - US-004
- Installed shadcn Alert component via `npx shadcn@latest add alert -y`
- Refactored MegalodonAlert.tsx to use shadcn Alert with destructive variant
- Replaced ⚠ emoji with AlertTriangle icon from lucide-react
- Drug name displayed via AlertTitle, message via AlertDescription
- Optional Intercept button preserved using AlertAction slot
- Left border accent preserved via className override (border-l-4 border-alert-red)
- Files changed: MegalodonAlert.tsx, ui/alert.tsx (new)
- **Learnings for future iterations:**
  - shadcn Alert v4 has AlertAction slot component for inline actions (positioned absolute top-right)
  - Alert uses data-slot attributes for internal styling hooks — keep these intact
  - `rounded-none border-0` overrides needed to strip default Alert border/radius when using custom border-left accent
  - MegalodonAlert is only used in DashboardHome.tsx — refactoring in-place avoids touching consumers
---

## 2026-03-21 - US-005
- Installed shadcn Collapsible via `npx shadcn@latest add collapsible -y`
- Refactored ModelRouterPanel.tsx: replaced manual useState expand/collapse with Collapsible/CollapsibleTrigger/CollapsibleContent, defaultOpen for initial expanded state
- Refactored LiveBrowserPreview.tsx: replaced manual useState expand/collapse with Collapsible components, starts collapsed (no defaultOpen)
- Removed unused useState imports from both components
- All existing content (pipeline steps, latency bars, sponsor badges, iframes) preserved
- completionFlash and gradientShift animations preserved in ModelRouterPanel
- Files changed: ModelRouterPanel.tsx, LiveBrowserPreview.tsx, ui/collapsible.tsx (new)
- **Learnings for future iterations:**
  - shadcn Collapsible v4 wraps `@base-ui/react/collapsible` — uses `defaultOpen` prop for initial state
  - Collapsible manages its own open/close state internally — no need for useState
  - For chevron rotation on open/close, use `[[data-panel-open]>&]:rotate-180` Tailwind selector targeting base-ui data attributes
  - The ▾/▸ text indicators can't easily toggle with Collapsible — use a static indicator or CSS-based approach instead
---

## 2026-03-21 - US-006
- Created custom Counter component in src/components/ui/counter.tsx (21st.dev registry returned 500 errors)
- Counter uses requestAnimationFrame with easeOutExpo easing for smooth number animation
- Refactored LiveMetricsBar.tsx: all 4 metric values use Counter component
- Pharmacies Scanned uses inline Counter for numerator with static denominator
- Savings Detected conditionally renders Counter (when value exists) or em dash placeholder
- tabular-nums font variant and status-based coloring preserved via style/className props
- Files changed: ui/counter.tsx (new), LiveMetricsBar.tsx
- **Learnings for future iterations:**
  - 21st.dev registry may be unreliable — be prepared to implement components manually
  - Counter component supports value, duration, className, style, and formatter props
  - For composite displays like "X/Y", use inline Counter without className and wrap in parent span for styling
  - formatVnd can be passed as Counter's formatter prop for currency formatting
---

